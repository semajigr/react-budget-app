import styled from "styled-components";

const StyledBadge = styled.div`
  display: flex;
  background-color: #7cc6fe;
  justify-content: space-between;
  padding: 50px 30px;
  margin-top: 40px;
`;

const Button = styled.button`
  padding: 10px 30px;
`;

export { StyledBadge, Button };
